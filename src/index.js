// console.log("Hello World!")

// task 1:
const myAge = 21; console.log(`My age is ${myAge} years old!`);

// task 2: 

// 'var' is optionally initialized and globally scoped
var manufacturer = "Mazda"
// 'let' is optionally initialized and block-scoped
let model = "Miata"
// 'const' needs to be initialized and cannot be reassigned using '=' and is also block-scoped
const modelYear = 1996
console.log(`my car is a ${manufacturer} ${model} from ${modelYear}.`)